---
layout: single
title:  "백준 2577 파이썬 문제풀이"
excerpt: "백준 2577번 풀이 count "

categories:
- Algorithm

tags:
- [Python, Algorithm]

date: 2022-04-25
published: true
---

### 문제

세 개의 자연수 A, B, C가 주어질 때 A × B × C를 계산한 결과에 0부터 9까지 각각의 숫자가 몇 번씩 쓰였는지를 구하는 프로그램을 작성하시오.

예를 들어 A = 150, B = 266, C = 427 이라면 A × B × C = 150 × 266 × 427 = 17037300 이 되고, 계산한 결과 17037300 에는 0이 3번, 1이 1번, 3이 2번, 7이 2번 쓰였다.

### 입력

첫째 줄에 A, 둘째 줄에 B, 셋째 줄에 C가 주어진다. A, B, C는 모두 100보다 크거나 같고, 1,000보다 작은 자연수이다.

### 출력

첫째 줄에는 A × B × C의 결과에 0 이 몇 번 쓰였는지 출력한다. 마찬가지로 둘째 줄부터 열 번째 줄까지 A × B × C의 결과에 1부터 9까지의 숫자가 각각 몇 번 쓰였는지 차례로 한 줄에 하나씩 출력한다.

### 내가 생각한 풀이

1. a,b,c를 정수로 입력 받고 곱한다음 스트링으로 바꾸고 리스트화 한다
2. 리스트에서 0~9 까지(여기서는 str)의 수를 샌 후 출력

사실 2에서 count를 사용한다면 아주 쉽게 풀 수 있는 문제지만 나는 count가 생각나지 않았다.

***그래서 내가 한 삽질의 결과물***
```python
a = int(input())
b = int(input())
c = int(input())
v = a*b*c
num_0 =0
num_1 =0
num_2 =0

v = list(str(v))
n = len(v)
for i in range(1,n+1)
    if a[i] == 0:
        num_0 += 1
    print(num_0)
    if a[i] == 1:
        num_1 += 1
    print(num_1)
    if a[i] == 2:
        num_2 += 1
    print(num_2)
```

이런식으로 9까지 다 써서 삽질을 했다.

내 뇌속에서는 이렇게 해도 될것같았고 count를 쓰면 되지 않을까 했지만 정확한 개념이 잡히지 않았다.

결과는 당연히 에러ㅎㅎ;;;;

뭔가 에러가 뜰것같았지만 그래도 해보고싶었다.

흠.. 지금 생각해도 count를 그냥 노가다로 쓴것같은데 어디서 에러가 난건지 아무나 알려줬으면 좋겠다 ㅠ

무튼 count를 사용한 정답 코드는 아래와 같다.

```python
a = int(input())
b = int(input())
c = int(input())
result = list(str(a*b*c))   #a,b,c를 정수로 입력받아 세 수의 곱을 구하고 str으로 변환하여 list화 한다.

for i in range(10):         #0~9까지 아래 코드를 반복
    print(result.count(str(i)))   #result리스트 안에서 i개수를 새서 출력
```

우선 정수를 문자열로 바꾸고 그것을 리스트화 하면 그 안에 수가 각각 index로 저장된다.

그 후 result.count를 통해 프린트 해주면 끝!!

### 문제를 틀린 이유

문자열 관련 함수 미숙지